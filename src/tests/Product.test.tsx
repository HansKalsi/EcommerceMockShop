import { describe, it } from "vitest";

describe("Product", () => {
    it("Product component renders", () => {
        // render(<Product />);
    });
    it("Product component renders correctly", () => {
        // const { getByRole } = render(<Product />);
        // expect(getByRole("image")).toBeInTheDocument();
        // expect(getByRole("title")).toBeInTheDocument();
        // expect(getByRole("description")).toBeInTheDocument();
        // expect(getByRole("price")).toBeInTheDocument();
        // expect(getByRole("button")).toBeInTheDocument();
    });
});
